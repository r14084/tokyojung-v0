{
  "version": 2,
  "functions": {
    "api/*.ts": {
      "runtime": "@vercel/node@3.0.27"
    },
    "api/**/*.ts": {
      "runtime": "@vercel/node@3.0.27"
    }
  },
  "buildCommand": "cd apps/customer-pwa && npm install && npm run build && cd ../staff-dashboard && npm install && npm run build && cd ../../ && npx prisma generate && mkdir -p public && cp -r apps/customer-pwa/dist/* public/ && mkdir -p public/staff && cp -r apps/staff-dashboard/dist/* public/staff/",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/staff(.*)",
      "dest": "/staff$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}